kind: Pod
metadata:
  name: hostpath-demo
spec:
  containers:
    - name: my-container
      image: busybox
      command: [ "sleep", "3600" ]
      volumeMounts:
        - name: host-data
          mountPath: /data/container
  volumes:
    - name: host-data
      hostPath:
        path: /data/host
        type: DirectoryOrCreate

step 1: check which node pod are running. and login with ssh
You’re logged in as: [ec2-user@ip-172-31-31-228 /]
cat /data/host/hello.txt


setp2:
enter into the container:
kubectl exec -it <NAME-OF-THE-MANIFEST> -- /bin/bash
now inside the container check




